model User {
  id        String    @id
  authInfo  AuthInfo?
  projects  Project[]
  createdAt DateTime  @default(now())
}

model AuthInfo {
  userId    String @id
  email     String @unique
  firstName String
  lastName  String
  user      User   @relation(fields: [userId], references: [id])
}
